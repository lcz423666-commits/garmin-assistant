# 佳明健康助手 项目档案

> **生成时间**：2026-04-17（最后更新：2026-04-18 21:20）  
> **审计执行人**：Claude Code  
> **部署环境**：阿里云服务器 `root@43.99.84.162`（SSH 私钥：`/Users/licongzhi/.ssh/aliyun_key`）  
> **项目路径**：`/root/garmin_assistant/`  
> **审计范围**：`/root/garmin_assistant/**` 及相关 `/root/.env`、`/root/.icu_*.json`

---

## 一、代码结构扫描

### 1.1 所有 `.py` 文件（仅项目目录，不含 openclaw）

| 文件路径 | 大小 | 最后修改时间 |
|---|---|---|
| `app/garmin_monitor.py` | 107,539 B | 2026-04-18 21:01 |
| `app/phase1_builder.py` | 120,060 B | 2026-04-04 12:52 |
| `app/report_flow.py` | 42,507 B | 2026-04-04 12:52 |
| `app/icu_cycling.py` | 30,788 B | 2026-04-17 08:55 |
| `app/llm_helper.py` | 32,015 B | 2026-04-04 12:52 |
| `app/activity_cleaner.py` | 12,384 B | 2026-03-22 11:55 |
| `app/icu_sleep.py` | 10,422 B | 2026-04-14 19:05 |
| `app/icu_turning_points.py` | 11,534 B | 2026-04-16 20:41 |
| `app/icu_turning_points_validator.py` | 10,410 B | 2026-04-16 20:41 |
| `app/app_config.py` | 7,019 B | 2026-04-01 00:14 |
| `app/garmin_cookie_client.py` | 7,294 B | 2026-03-31 22:06 |
| `app/knowledge_helper.py` | 7,668 B | 2026-04-03 15:53 |
| `app/garmin_storage.py` | 5,143 B | 2026-03-20 16:50 |
| `app/sleep_cleaner.py` | 4,791 B | 2026-03-21 20:48 |
| `app/chart_renderer.py` | ~9,800 B | 2026-04-18 10:05 |
| `app/goal_tracker.py` | 新增 | 2026-04-18 |
| `app/user_profile.py` | 新增 | 2026-04-18 |
| `app/ride_history.py` | 新增 | 2026-04-18 |
| `weekly_profile_update.py` | 新增 | 2026-04-18 |
| `app/chart_config.py` | 666 B | 2026-04-03 20:49 |
| `app/chart_decider.py` | 3,693 B | 2026-04-03 20:01 |
| `app/chart_storage.py` | 1,736 B | 2026-04-03 20:08 |
| `analysis.py` | 26,973 B | 2026-04-03 15:30 |
| `weekly_monthly.py` | 22,192 B | 2026-04-03 20:01 |
| `experiment.py` | 16,505 B | 2026-04-03 09:52 |
| `baseline.py` | 11,733 B | 2026-03-31 20:42 |
| `daily_snapshot.py` | 7,354 B | 2026-03-31 20:41 |
| `user_identity.py` | 1,302 B | 2026-03-31 20:40 |
| `scripts/add_user_and_onboard.py` | 21,049 B | 2026-03-31 18:38 |
| `scripts/generate_daily_report.py` | 26,979 B | 2026-03-21 16:42 |
| `scripts/health_snapshot.py` | 15,284 B | 2026-03-22 16:12 |
| `scripts/probe_data.py` | 12,162 B | 2026-03-31 20:43 |
| `scripts/collect_review_samples.py` | 16,937 B | 2026-03-20 16:50 |
| `scripts/export_garmin_samples.py` | 17,660 B | 2026-03-20 16:50 |
| `scripts/init_garmin_token.py` | 2,834 B | 2026-03-26 11:53 |
| `scripts/backfill.py` | 1,678 B | 2026-03-31 20:43 |
| `scripts/batch_test.py` | 4,899 B | 2026-03-30 19:18 |
| `scripts/test_*.py`（14个测试脚本） | — | 2026-03-30~2026-04-18 |
| `backup/patch.py` / `patch_C.py` / `patch_monitor.py` | — | 2026-03-17 |

### 1.2 各核心文件职责与函数清单

#### `app/garmin_monitor.py`（主监控进程）
**职责**：守护进程入口，每轮轮询所有启用用户，拉 Garmin 数据→判断新活动/睡眠→调 LLM→推送微信，同时驱动 ICU 骑行和睡眠分析（仅丛至）。

主要函数：
- `load_enabled_users()` — 从 users.json 读取 enabled 用户
- `sync_runtime_users()` — 动态同步用户缓存
- `load_state/save_state()` — 每用户状态文件读写
- `login_garmin()` — 使用 token 目录或密码登录 Garmin
- `push_to_wechat()` — PushPlus 微信推送（含图表附件）
- `current_check_strategy()` — 判断是否高频时段（05-09h 每5分钟，其余每20分钟）
- `get_feishu_token/feishu_write_push_record/feishu_write_run_log()` — 飞书多维表格日志
- `feishu_enabled()` — 飞书是否启用
- `update_sleep_health_state/update_evening_push_state()` — 状态文件更新
- `bj_now()` — 北京时间工具
- `build_pushplus_payload()` — 构建推送 HTML

依赖：`garminconnect`（Garmin SDK）、`requests`（HTTP）、`openai`（LLM via SiliconFlow）

---

#### `app/phase1_builder.py`（数据构建层，120 KB，最大文件）
**职责**：构建所有 LLM payload（睡眠、运动、周报、初始报告、冷启动快照、基线），是分析逻辑的核心。

主要函数（部分）：
- `build_sleep_payload()` — 构建睡眠分析 payload（含 SPO2 告警、HRV、血氧连续性检测）
- `build_activity_payload()` — 构建运动分析 payload（含骑行专项、左右平衡、踏频、TSS/IF）
- `build_weekly_payload()` — 周报 payload
- `build_initial_7d_summary_payload()` — 新用户接入后7天初步分析
- `build_initial_summary_payload()` — 30天综合分析
- `build_cold_start_snapshot()` — 冷启动快照
- `build_user_baseline_payload()` — 30天基线刷新
- `_sleep_spo2_alert_context()` — SPO2 连续告警检测（单晚/多晚/持续）
- `_sleep_forced_alerts()` — 强制告警生成
- `_activity_forced_alerts()` — 运动强制告警
- `summarize_cycling_specific()` — 骑行专项摘要（功率区间、踏频、前中后段分析）
- `build_performance_highlights()` — VO2Max/FTP 提升亮点
- `derive_weekly_intensity_minutes()` — WHO 每周强度分钟数

---

#### `app/report_flow.py`（报告流程编排）
**职责**：编排晨报、晚间提醒、运动快报的生成流程；含知活动-issue 追踪（已知问题避免重复提醒）。

主要函数：
- `generate_new_morning_report()` — 晨报生成（调 build_sleep_payload + LLM）
- `generate_evening_report()` — 晚间提醒生成
- `enrich_activity_payload()` — 为运动分析补充额外字段
- `check_evening_triggers()` — 判断晚间提醒触发条件
- `build_morning_prompt/build_evening_prompt()` — 构建用户侧 prompt
- `call_custom_llm()` — 调用 LLM
- `update_known_activity_issues()` — 更新已知问题列表
- `maybe_build_morning_chart/maybe_build_activity_chart()` — 图表生成决策

---

#### `app/llm_helper.py`（LLM 调用封装）
**职责**：封装 OpenAI 兼容接口调用，管理所有 system prompt，执行 LLM 分析。

主要函数：
- `analyze_with_llm(payload, mode)` — 主入口，按 mode 选择 prompt
- `build_user_prompt()` — 构建用户侧消息
- `clean_output()` — 清理 LLM 输出（去分隔线、字数标注等）
- `_humanize_duration_fields()` — 时长字段人话化（分钟→X小时Y分钟）
- `_soften_conflicting_sleep_praise()` — 矛盾睡眠赞美语气软化

---

#### `app/icu_cycling.py`（ICU 骑行深度分析）
**职责**：从 Intervals.icu 拉最新骑行活动详细数据（streams），调用拐点检测 + 高德逆地理编码 + Open-Meteo 风向分析 + 历史对比，生成骑行深度分析并推送（仅限用户"丛至"）。推送使用 HTML 模板，图表附件置于文字前。

关键变量：
- `TARGET_USER_NAME = "丛至"`
- `ACTIVITIES_LOOKBACK_DAYS = 7`
- streams 拉取类型：`watts, heartrate, cadence, distance, velocity_smooth, altitude, latlng, time`

推送流程（`check_and_push_cycling()`）：
1. 拉最新骑行 activity + streams + wellness
2. 拐点检测（高功率段/脱钩点/衰减段）+ 高德地理编码
3. Open-Meteo 风向分段分析
4. 历史可比骑行查询（`ride_history.find_comparable_rides`）
5. LLM 生成分析文本
6. 渲染骑行区间分布图（`render_cycling_zones_chart`），生成 HTTPS 图片 URL
7. HTML 推送（图在上、文字在下）
8. 写入 rides.db 历史库
9. 提取目标（`goal_tracker`）+ 记录观察

---

#### `app/icu_sleep.py`（ICU 睡眠深度分析）
**职责**：从 Intervals.icu 拉 wellness 数据（睡眠 HRV、TSB），生成睡眠深度分析并推送（仅限用户"丛至"）。

关键变量：
- `TARGET_USER_NAME = "丛至"`
- `WELLNESS_LOOKBACK_DAYS = 3`

主要函数：
- `check_and_push_sleep()` — 主流程（推送前注入今日待执行目标 + 用户长期画像，推送后提取目标 + 记录观察）
- `extract_sleep_analysis_data()` — 从 ICU wellness 条目提取睡眠字段
- `select_latest_wellness()` — 选最近未推送条目
- `_build_analysis_prompt(payload, pending_goals, profile_summary)` — 构建 ICU 睡眠分析 prompt（含目标回顾和画像上下文）
- `_generate_analysis()` — 调 LLM

---

#### `app/icu_turning_points.py`（骑行拐点检测算法）
**职责**：接收 streams 数据（功率/心率/踏频/距离），检测高功率段、心率-功率脱钩点、功率衰减点，输出带里程位置的拐点清单。

主要函数：
- `detect_high_power_segments(streams, ftp)` — 持续高功率段（≥FTP×0.95，5分钟窗口）
- `detect_decoupling_points(streams)` — 心率-功率脱钩点（二半段对比，涨幅≥15%触发）
- `detect_power_decay(streams)` — 功率衰减（三等分比较，跌幅>15%触发）
- `stream_map_from_payload()` — streams payload 解析
- `align_streams()` — 多流对齐到最短长度
- `_window_stats()` — 5分钟滑动窗口统计

---

#### `app/icu_turning_points_validator.py`
**职责**：拐点检测算法的单元测试/验证器，生成合成数据场景验证三个检测函数。

---

#### `app/activity_cleaner.py`
**职责**：清洗和标准化 Garmin 原始活动数据，构建归一化 activity record。

主要函数：
- `clean_activity()` — 原始 activity dict 清洗
- `normalize_activity()` — 归一化（含骑行专项、splits、badges）
- `build_full_activity_record()` — 合并 activity + detail + splits + badges
- `is_cycling_type()` — 判断是否骑行类型

---

#### `app/app_config.py`（配置与敏感数据处理）
**职责**：加载 system.json / users.json，提供全局路径常量，提供文本脱敏工具。

关键功能：
- `load_system_config()` / `load_users()` — 配置读取（LRU 缓存）
- `sanitize_text()` — 正则脱敏（email、JWT、Bearer token、secret 赋值）
- `sanitize_export_value()` — 结构体深度脱敏
- `mask_identifier()` / `mask_email()` — 标识符遮盖

---

#### `app/garmin_cookie_client.py`
**职责**：针对 Garmin CN（connectapi.garmin.cn）的 Cookie 登录客户端，绕过 OAuth 流程，支持国内账号。

---

#### `app/garmin_storage.py`
**职责**：用户数据持久化层，按 `用户名/分类/record_key` 目录结构存储 JSON 包。

---

#### `app/knowledge_helper.py`
**职责**：加载运动知识库（`knowledge/*.md`），按运动类型注入相关知识段落到 system prompt，增强 LLM 专项分析能力。

---

#### `app/chart_renderer.py` / `chart_decider.py` / `chart_storage.py` / `chart_config.py`
**职责**：Matplotlib 图表生成，决策哪些场景需要附图，管理图表文件存储和公开 URL 构建。图表均通过 HTTPS（`https://43.99.84.162.sslip.io/charts/...`）托管对外访问。

当前已实现的图表类型：
- `render_line_chart()` — 折线趋势图（通用）
- `render_sleep_structure_chart()` — 睡眠结构甜甜圈图
- `render_cycling_zones_chart()` — 骑行训练区间分布图（2026-04-18 新增）：两行堆叠横条，上方功率区间（Z1-Z7），下方心率区间（Z1-Z5），每段显示分钟数

---

#### `app/goal_tracker.py`（目标追踪，2026-04-18 新增）
**职责**：从分析文本中提取前瞻性目标（如"明天建议 Z2 骑行"），存入 `daily_goals.json` 队列；第二天检查执行情况；同时维护 `observations_log.json`（每日观察日志，90 天滚动）。

主要函数：
- `extract_goals_from_text(text, source)` — LLM 提取目标（temperature=0.1），返回带 `target_date` 的 list
- `save_goals(goals)` — 按日期去重写入 `data/丛至/daily_goals.json`
- `get_pending_goals(target_date)` — 查询指定日期（默认今天）的待执行目标
- `append_observation(obs_text, source)` — 追加一条观察到 `observations_log.json`（每 source 每天限一条）

---

#### `app/user_profile.py`（用户长期画像，2026-04-18 新增）
**职责**：读写 `data/丛至/user_long_term_profile.json`，为 prompt 提供用户个性化背景摘要。

主要函数：
- `load_profile()` / `save_profile(profile)` — 读写 JSON 文件
- `profile_summary_for_prompt()` — 返回多行字符串（训练模式、睡眠模式、恢复特征、个人观察、骑行进展），注入 ICU 睡眠 prompt

---

#### `app/ride_history.py`（骑行历史库，2026-04-18 新增）
**职责**：SQLite 数据库 `data/丛至/rides.db`，存储每次骑行关键指标，支持历史可比骑行查询。

主要函数：
- `init_db()` — 建表（首次运行）
- `upsert_ride(activity_id, ride_data, wellness_data)` — 写入/更新一条骑行记录
- `find_comparable_rides(distance_km, if_value, exclude_activity_id, limit)` — 查找距离 ±20%、IF ±15% 的历史骑行（最多10条）
- `compute_comparison(current_ride, comparables)` — 计算功率/效率/脱钩与历史均值的差值
- `build_cycling_progression_summary(days)` — 生成最近 N 天骑行进展摘要（供 weekly_profile_update 调用）

---

#### `weekly_profile_update.py`（画像周更新，2026-04-18 新增）
**职责**：每周日早 7 点（cron `0 7 * * 0`）读取 observations_log + 当前画像 + rides.db 骑行进展，由 LLM 综合合成新画像，写回 `user_long_term_profile.json`。支持 `--days` 和 `--dry-run` 参数。

输出字段：`training_patterns`、`sleep_patterns`、`recovery_characteristics`、`personal_observations`、`cycling_progression`

---

#### `app/sleep_cleaner.py`
**职责**：清洗 Garmin 原始睡眠数据，归一化为统一格式。

---

#### `weekly_monthly.py`
**职责**：生成周报和月报，聚合历史 sleep/activity 包数据。

---

#### `analysis.py` / `experiment.py` / `baseline.py` / `daily_snapshot.py`
**职责**：独立运行的分析/实验/基线脚本，供手动调用或 cron 触发，不在主监控循环内。

---

### 1.3 Git 仓库状态

**已有 Git 仓库**。`/root/garmin_assistant/` 当前位于 `main` 分支。

最近提交：
- `5d71926 docs: 更新项目档案——修正丛至说明、补充 git 初始化记录`
- `51ccfe7 chore: 从版本控制中移除 .out 运行输出文件`
- `fe51d87 chore: 初始化 git 仓库，归档项目现状 (2026-04-17)`

当前代码管理风险（2026-04-18 21:02）：
- 线上仍有未提交改动：`app/chart_renderer.py`、`app/garmin_monitor.py`、`app/icu_cycling.py`、`app/icu_sleep.py`、`app/knowledge_helper.py`、`app/report_flow.py`、`knowledge/sleep_health.md`、`scripts/cleanup_charts.sh`、`scripts/prune_chart_images.sh`
- 线上仍有未跟踪新增文件：`app/goal_tracker.py`、`app/ride_history.py`、`app/user_profile.py`、`knowledge/cycling_training_plan.md`、`weekly_profile_update.py`、`scripts/test_chart_cleanup_layout.py`、`scripts/test_health_runtime_pruning.py`

**建议**：修复验证稳定后尽快提交一次“可回滚快照”，避免生产代码继续处于漂移状态。

---

## 二、大模型接入现状

### 2.1 当前使用模型

| 项 | 值 |
|---|---|
| **厂商** | SiliconFlow（硅基流动） |
| **模型名** | `Pro/moonshotai/Kimi-K2.5` |
| **base_url** | `https://api.siliconflow.cn/v1` |
| **接口协议** | OpenAI 兼容（使用 `openai` Python SDK） |
| **配置位置** | `/root/garmin_assistant/config/system.json` → `llm.model` |

**模型切换逻辑**：无多模型并存。`LLM_MODEL` 在 `llm_helper.py` 启动时从 `system.json` 读取一次，全程使用同一模型。切换只需修改 `system.json`。

ICU 骑行/睡眠分析（`icu_cycling.py` / `icu_sleep.py`）通过 `garmin_monitor.py` 的 `analyze_with_llm()` 调用同一模型。

---

### 2.2 System Prompts 原文

#### SLEEP_PROMPT（睡眠晨报）

```
你是一个懂 Garmin 数据和训练恢复的微信陪伴型教练。

你的任务不是重新做分析，而是把程序已经整理好的睡眠判断，写成一条有重点深度的睡眠晨报。

必须遵守：
- 目标长度 400-650 字，最多 650 字
- 优先直接使用 `preferred_opening` 开头；如果不是早上，不要硬写"下午好/晚上好"
- 如果 `preferred_opening` 已经给了称呼后的判断句，正文第一段必须直接沿用这句，不要只保留称呼后再自己改回泛泛开头
- 开头第一句必须同时包含总体判断和最关键问题，优先直接点名最低血氧、HRV、静息心率等具体异常，不要先铺一句模糊判断再卖关子
- 全文不要出现"有一个明确提醒不能忽略"这句话，也不要用"但有个提醒要说""先说一个问题"这类悬念式模板句
- 面向用户展示任何时长时都统一人话化：大于等于 60 分钟写成"X小时Y分钟"，小于 60 分钟写"X分钟"
- 结构顺序必须是：开头 -> 总判断 -> 原因解释 -> 亮点 -> 问题点/提醒 -> 跟问题点绑定的建议 -> 连续趋势一句话
- 至少写出 2-4 个关键依据，不能只说"恢复不错"或"保持稳定"
- 如果 `forced_alerts` 不为空，必须把每一条都写进正文，不能吞掉
- 如果有 `user_stage`，要优先按阶段来写，而不是默认自己已经很懂用户
- `user_stage=observation`：只讲这晚本身 + 很轻的初步判断，不要写"你平时""你最近一直""你的一贯水平"，也不要做强基线对比
- `user_stage=early`：可以少量参考最近7天或最近一周，但要带"初步判断""从目前这段时间看"这类口气，不要写得太满
- `user_stage=mature`：可以自然引用最近30天常态和"你平时"的表达，但 baseline 仍然只用于帮助判断，不要逐条复述
- `user_stage` 只能影响整体语气和判断口径，不能压掉明显异常的提醒力度
- 如果出现明显异常值，比如最低血氧明显偏低、HRV 明显低于平衡区间、静息心率明显高于常态，这些提醒仍然要保留足够强度
- 如果 `spo2_alert_context.guidance_level=observe`，最低血氧可以按偶发波动来写，建议以继续观察为主，但要明确这是偶发下探
- 如果 `spo2_alert_context.guidance_level=escalated`，必须明确写出这已经是反复出现的信号，可能和夜间呼吸受阻有关；建议里自然提示关注打鼾、憋醒/喘醒、白天困倦、晨起头痛，且不要把主要建议写成一句轻飘飘的"继续留意"
- 如果 `spo2_alert_context.template_mode=persistent_issue`，整条文案都不要再按"单晚异常模板"来写；开头第一句就要点名这是反复出现的问题，不要写得像第一次看到
- 如果 `spo2_alert_context.recent_occurrence_text` 或 `spo2_alert_context.consecutive_occurrence_text` 不为空，正文里要优先明确写出"最近一周已经第几次出现""已经连续第几晚出现"
- 如果开头已经把主问题和连续性说清楚，后文不要再换说法重复"不是偶发波动""需要按连续异常处理""需要重视"这类同一个判断；后文改写它意味着什么、以及下一步怎么做就够了
- 全文禁止 markdown，不要加粗，不要加小标题
- 更像微信消息，不要像健康报告
- 少一点讲课感，少用"我们来看""值得关注""建议你"这类教练腔
- 输出格式要求：不要在文案中包含分隔线（---）、字数统计（如"约370字"）、或任何元标注。文案结尾应该是一个完整的、有实际内容的句子，不要用单独一两个词收尾。
```

---

#### ACTIVITY_PROMPT（运动快报）

```
你是一个懂 Garmin 数据和训练恢复的微信陪伴型教练。

你的任务不是重新做全量分析，而是把程序整理好的训练判断，写成一条有重点深度的运动快报。

必须遵守：
- 目标长度 450-700 字，最多 700 字
- 面向用户展示任何时长时都统一人话化：大于等于 60 分钟写成"X小时Y分钟"，小于 60 分钟写"X分钟"
- 结构顺序必须是：一句话训练定性 -> 原因解释 -> 专项亮点 -> 专项问题/提醒 -> 跟问题点绑定的恢复建议 -> 连续视角
- 至少写出 2-4 个关键依据，不能只说"负荷不轻、恢复优先"
- 只要正文里用了判断型表述，就必须给出对应关键数据，不能只下结论不给依据
- 如果有 `required_metric_mentions`，正文要优先自然带出其中的平均功率和平均心率
- 如果写"功率稳定""心率更活跃""负担偏高""节奏不错"这类判断，必须同时引用对应数值，优先用 `reason_points`、`priority_issues.evidence`、`key_metrics`、`cycling_specific_summary`
- 数字要自然嵌在句子里，不要堆成数据表，但平均功率和平均心率尽量常规保留
- 如果有 `user_stage`，要优先按阶段来写，而不是默认自己已经很懂用户
- `user_stage=observation`：主要讲这次训练本身，不要讲太多"你平时"，也不要装得很懂他的长期运动特征
- `user_stage=early`：可以和最近几次或最近一周比，但仍然要强调"初步看""从目前这段时间看"
- `user_stage=mature`：可以正式参考30天运动基线、主运动定位和专项线，但 baseline 仍然只用于帮助判断，不要逐条复述
- `user_stage` 只能影响整体语气和判断口径，不能压掉明显异常的提醒力度
- 如果出现明显异常，比如训练负荷显著超常、后程衰减很明显、左右失衡反复偏大或其他高优先级问题，这些提醒仍然要保留足够强度
- 如果有 `baseline_view`，它的主要作用是帮你判断"这次训练对这个用户来说偏轻、接近常态，还是偏重"
- 不要把 `baseline_view` 里的字段逐条复述给用户，也不要把"高于30天常态、低于30天常态、接近30天常态"全部展开成报告
- 每次最多只挑 1-2 个最有价值的基线对比写进正文，优先翻成人话，不要直接念字段名
- `main_sport_positioning` 和 `cycling_specific_baseline` 主要用于帮助判断，不要整块翻译给用户
- baseline 主要用于帮助下判断，不是增加解释负担；最终文案仍然要像微信消息，不像基线报告
- 如果有 `continuity_context.continuity_sentence_hint`，正文里自然带 1 句连续感表达就够了，不要写成训练回顾小结
- `continuity_context.recent_issue_memory`、`continuity_context.continuity_summary`、`continuity_context.improvement_after_last_advice` 只挑 1 句最有价值的写出来
- 优先从平均功率、平均心率、IF、TSS/负荷、NP vs 平均功率、左右平衡、踏频、区间结构、补水差额、前中后段节奏变化里挑 2-3 个最有价值的点
- 只把 `priority_issues` 里的前 1-2 个问题点写进正文，不要把所有触发项都列出来
- `secondary_issues` 和非优先问题点不要写进正文，也不要在结尾补充一串"另外还要注意"
- 少一点讲课感，不要用"主要看四个点""没白费""被打断了四成"这类讲评腔或硬量化说法
- 少讲原理，少用"建立耐力和燃脂基础""关键反而是"这类教练讲评腔
- 不要写"最值钱的亮点"，改成"最值得肯定的地方"
- 不要把明明是这次训练写成"昨天""前天""昨晚"；活动时间一律优先写成"这次训练""这次活动"，必要时只用 payload 里的绝对日期，不要自己发明相对日期
- 不要加小标题，不要用 `**`
- 不要提昨晚睡眠
- 禁止 markdown
- 输出格式要求：不要在文案中包含分隔线（---）、字数统计（如"约370字"）、或任何元标注。文案结尾应该是一个完整的、有实际内容的句子，不要用单独一两个词收尾。
```

**ACTIVITY_PROMPT_EXTRA**（附加在 ACTIVITY_PROMPT 之后）：

```
## 新增数据维度

你现在收到的数据中包含以下新维度，如果有值（不为 null），必须在分析中使用：

- training_status：当前训练状态。必须在分析中提及，告诉用户这次训练对整体训练状态意味着什么。
- vo2max 和 fitness_age：如果有值，在分析中提及。VO2 Max 的变化趋势是用户很关心的长期指标。
- cycling_ftp：如果有值且是骑行运动，可以用来评估功率表现，比如平均功率相对于 FTP 的百分比。
- performance_highlights：如果这个列表非空，说明这次活动后出现了明确的长期指标提升，例如 VO2 Max 或 FTP 上升。专项亮点第一句优先直接写这里最重要的一条，不要把它埋到后文。
- performance_priority_point：如果这个字段有值，专项亮点第一句优先直接用它。
- weekly_intensity_minutes / who_completion_pct / who_gap_minutes / who_should_mention：只有当 `who_should_mention=true` 且 `who_gap_minutes` 大于 0 时，才用一句话说明"这次训练后你本周的活动强度分钟数已到 XX 分钟，还差 XX 分钟达到世界卫生组织每周150分钟建议"。如果已经达到或超过150分钟，不要提这个指标。
- 活动强度分钟数必须用"分钟"为单位表达，不要换算成小时。正确："132分钟"。错误："2小时12分钟"。
- endurance_score 和 hill_score：如果有值且和运动类型相关，作为长期进步的佐证。
- 如果当天有多信号异常预警（anomaly_alert=true），在运动分析末尾也要提醒用户身体状态不佳，格外注意恢复。

## 重复提醒规则

- 输入里的 known_activity_issues.rules 表示已经提醒过的问题；known_activity_issues.repeat_rules 表示已经反复提醒过的问题。
- 对于 repeat_rules 里的问题，默认不要再次展开分析，也不要把它写成这次训练的重点。
- 只有当数据显示问题明显恶化，或者这次问题仍然保留在最高优先级时，才重新展开提醒。

## 额外要求

1. 关于补水：只有存在真实补水记录时才允许提补水。
2. 不要机械罗列 extra_training_data，要只挑对这次训练最有帮助的 1-3 个点带进去。
3. 语气上不要夸用户，不要写成鼓励打鸡血或发奖状。只陈述数据本身体现出的积极或消极变化。
```

---

#### WEEKLY_PROMPT（7天周报）

```
你是一个懂 Garmin 数据和训练恢复的微信陪伴型教练。

你的任务是基于程序整理好的过去7天摘要，写一条像微信复盘一样的"过去7天固定总结"，只讲用户过去7天真实发生了什么。

必须遵守：
- 目标长度 600-900 字，最多 900 字
- 面向用户展示任何时长时都统一人话化：大于等于 60 分钟写成"X小时Y分钟"，小于 60 分钟写"X分钟"
- 结构顺序必须是：过去7天总体判断 -> 睡眠与恢复总结 -> 训练节奏总结 -> 最值得注意的变化 -> 接下来7天建议
- [... 完整内容见 llm_helper.py WEEKLY_PROMPT 变量 ...]
- 不要输出分隔线，不要自我解释
- 禁止 markdown
```

#### INITIAL_7D_PROMPT（新用户7天初步分析）

结构顺序：开场一句 → 最近7天整体状态 → 睡眠与恢复初步特征 → 训练与运动初步特征 → 目前最值得注意的一个点 → 接下来会继续重点关注什么。字数：400-700。

#### INITIAL_30D_PROMPT（新用户30天综合分析）

结构顺序：开场一句 → 最近30天整体状态 → 睡眠与恢复特征 → 训练与运动特征 → 最值得注意的一个特点 → 接下来会重点关注什么。字数：600-1000。

---

### 2.3 API Key 配置位置

| Key | 配置文件 | 遮盖展示 |
|---|---|---|
| LLM API Key（SiliconFlow） | `/root/garmin_assistant/config/system.json` → `llm.api_key` | `sk-q****wswt` |
| ICU API Key | `/root/.env` → `ICU_API_KEY` | `1yq3****hk5j` |
| 高德地图 API Key | `/root/.env` → `AMAP_API_KEY` | `689d****cd30` |
| 飞书 App Secret | `config/system.json` → `feishu.app_secret` | `6yZk****LSse` |
| 飞书 App ID | `config/system.json` → `feishu.app_id` | `cli_****dbde` |
| 飞书 App Token | `config/system.json` → `feishu.app_token` | `UEA8****Nn0e` |
| PushPlus 管理员 Token | `config/system.json` → `pushplus.admin_token` | `5965****d002` |

---

## 三、数据源与第三方 API

### 3.1 Garmin Connect（`garminconnect` 库）

- **已接入**
- **接入文件**：`app/garmin_monitor.py`（`login_garmin()`）
- **用途**：拉取每日睡眠、活动列表、活动详情、body battery、HRV、血氧、步数、训练状态、强度分钟数等
- **凭据存储**：
  - 主要：`/root/garmin_tokens/{用户名}/` 目录下的 token 文件（garth token cache）
  - 备用：`config/users.json` 中的 `garmin_email` + `garmin_password`（明文存储，见安全问题）
  - 国内账号：另有 `app/garmin_cookie_client.py` 的 Cookie 登录方式（`garmin_is_cn=true` 用户）

### 3.2 Intervals.icu API

- **已接入**（仅限用户"丛至"）
- **接入文件**：`app/icu_cycling.py`、`app/icu_sleep.py`
- **用途**：骑行 streams 数据（功率/心率/踏频）、wellness 条目（睡眠 HRV/TSB）
- **凭据存储**：`/root/.env` → `ICU_ATHLETE_ID=i231729`、`ICU_API_KEY=1yq3****hk5j`
- **Base URL**：`https://intervals.icu/api/v1`
- **调用鉴权**：HTTP Basic Auth（`API_KEY:` + api_key）

### 3.3 高德地图 API

- **已接入**
- **接入文件**：`app/icu_cycling.py`（`AMAP_REGEOCODE_URL`）
- **用途**：将 GPS 坐标（latlng）逆地理编码为里程位置文字描述，配合拐点检测结果输出人类可读的位置标注
- **凭据存储**：`/root/.env` → `AMAP_API_KEY=689d****cd30`
- **接口**：`https://restapi.amap.com/v3/geocode/regeo`（逆地理编码）

### 3.4 Open-Meteo

- **已接入**（2026-04-18）
- **接入文件**：`app/icu_cycling.py`（`build_weather_analysis()`、`fetch_open_meteo_weather()`）
- **用途**：按活动日期 + 起点坐标拉逐小时气象数据（风向、风速、温度、湿度、天气状况、降水），与 GPS 轨迹按每 2km 分段计算顺/侧/逆风关系
- **接口**：近 7 天用 Forecast API，更早用 Archive API，均免费无需 API Key
- **输出**：`weather_analysis` 字段注入 LLM prompt，含分段详情 + 主导风力 + 气象概况

### 3.5 PushPlus 微信推送

- **已接入**
- **接入文件**：`app/garmin_monitor.py`（`push_to_wechat()`）
- **用途**：向用户微信推送睡眠晨报、运动快报、晚间提醒、周报
- **凭据存储**：
  - 管理员 token：`config/system.json` → `pushplus.admin_token`
  - 各用户 token：`config/users.json` → `pushplus_token` 或 `friend_token`（明文）
- **推送模式**：`self`（用户自己的 token）或 `friend`（管理员代推）

### 3.6 飞书多维表格

- **已接入**
- **接入文件**：`app/garmin_monitor.py`（`feishu_write_push_record()`、`feishu_write_run_log()`）
- **用途**：记录推送日志（push_table）和运行日志（log_table）到飞书多维表格，供运营审查
- **凭据存储**：`config/system.json` → `feishu`（app_id、app_secret、app_token 均明文）
- **鉴权**：tenant_access_token 动态获取

---

## 四、运行时状态

### 4.1 Crontab 定时任务

```
CRON_TZ=Asia/Shanghai
TZ=Asia/Shanghai
*/15 * * * *  /usr/bin/python3 /root/garmin_assistant/app/garmin_monitor.py >> /root/garmin_assistant/logs/garmin.log 2>&1
10 21 * * *   /usr/bin/python3 /root/garmin_assistant/scripts/generate_daily_report.py --json >> /root/garmin_assistant/logs/daily_report.log 2>&1
0 7 * * 0     /usr/bin/python3 /root/garmin_assistant/weekly_profile_update.py >> /root/garmin_assistant/logs/profile_update.log 2>&1
```

- **主监控**：每15分钟触发一次（cron 触发），但进程内置 PID 锁，实际由进程自身睡眠控制轮询间隔：
  - 05:00–09:00（高频时段）：每5分钟轮询一次
  - 其余时段：每20分钟轮询一次
- **日报脚本**：每天21:10执行一次
- **画像周更新**：每周日7:00执行一次

### 4.2 状态文件结构（`.garmin_monitor_state_{用户}.json`）

每用户一个状态文件，位于 `/root/garmin_assistant/state/`，包含以下字段（不含具体数值）：

| 字段 | 含义 |
|---|---|
| `last_sleep_date` | 最近已推送的睡眠日期 |
| `last_activity_ids` | 已推送的活动 ID 列表（去重） |
| `last_weekly_report_key` | 最近周报的标识 |
| `last_monthly_report_key` | 最近月报的标识 |
| `initial_onboarding_status` | 新用户接入阶段状态 |
| `initial_backfill_completed_at` | 初始回填完成时间 |
| `initial_backfill_sleep_days` | 回填睡眠天数 |
| `initial_backfill_activity_count` | 回填活动数 |
| `cold_start_stage` | 冷启动阶段（observe/early/mature） |
| `effective_sleep_days` | 有效睡眠样本天数 |
| `effective_activity_days` | 有效运动样本天数 |
| `cold_start_snapshot` | 冷启动快照数据 |
| `initial_7d_summary_sent_at` | 7天初步分析发送时间 |
| `initial_30d_summary_sent_at` | 30天综合分析发送时间 |
| `last_successful_cycle_at` | 最近一次成功轮询时间 |
| `last_cycle_error` | 最近一次轮询错误 |
| `last_sleep_check_at/date/result/reason` | 睡眠检查记录 |
| `last_sleep_push_at/date/result/reason/title` | 睡眠推送记录 |
| `last_evening_push_at/date/type/result/reason/title` | 晚间推送记录 |

**ICU 骑行状态**（`/root/.icu_cycling_state.json`）：
- `last_activity_id`：最近已推送的 ICU 活动 ID（`i140799074`，4/18 VirtualRide）
- `updated_at`：上次更新时间（`2026-04-18T19:19:00`）

**ICU 睡眠状态**（`/root/.icu_sleep_state.json`）：
- `last_date`：最近已推送的睡眠日期（`2026-04-17`）
- `updated_at`：上次更新时间（`2026-04-17T09:43:19`）

### 4.3 SQLite 数据库

**`data/丛至/rides.db`**（2026-04-18 建立）

骑行历史库，表结构：

| 字段 | 含义 |
|---|---|
| `activity_id` | ICU 活动 ID（主键） |
| `date` | 活动日期 |
| `distance_km` | 距离 |
| `duration_min` | 移动时长 |
| `elevation_m` | 爬升 |
| `avg_power` / `np` | 平均功率 / 标准化功率 |
| `ftp_at_time` | 当时 FTP |
| `if_value` | 强度因子 |
| `vi` / `ef` | 变异指数 / 效率因子 |
| `tss` | 训练压力分 |
| `decoupling` | 有氧脱耦率 |
| `avg_hr` / `avg_cadence` | 平均心率 / 踏频 |
| `tsb_at_time` | 当时 TSB |

可比骑行查询条件：距离 ±20%、IF ±15%，最多返回 10 条历史记录。

### 4.4 garmin.log 近300行分析

- **最近一次成功运行时间**：2026-04-17 16:16（cron 触发，PID=3002942 进程正在运行）
- **进程状态**：`PID=2934438` 自 2026-04-16 20:47 重启为 `PID=2934438`，2026-04-17 15:45 后切换为 `PID=3002942`，均正常
- **重复错误**：`connectapi.garmin.cn` 读超时（ReadTimeoutError），影响国内 CN 账号（如十月的麦子），每次约占1-2个轮询周期，之后自动恢复
- **十月的麦子**：持续出现 `睡眠数据暂无（dailySleepDTO 为空）`，CN 域名偶发超时，睡眠数据长期缺失（最近已推日期 2026-04-14）

**各用户最近一次推送时间**（睡眠晨报 | 晚间提醒）：

| 用户 | 最近睡眠推送 | 最近晚间推送 | 状态 |
|---|---|---|---|
| 丛至 | 2026-04-17 | 2026-04-16 | ✅ 正常（enabled） |
| Kevin | 2026-04-17 | 2026-04-16 | ✅ 正常（enabled） |
| 杨 | 2026-04-16 | 2026-04-16 | ✅ 正常（enabled） |
| 未央 | 2026-04-17 | 2026-04-11 | ✅ 正常（enabled） |
| 十月的麦子 | 2026-04-14 | 2026-04-16 | ⚠️ 睡眠数据持续缺失 |
| Cyanide | 2026-04-17 | 2026-04-16 | ✅ 正常（enabled） |
| 闲云野鹤 | 2026-04-17 | 2026-04-16 | ✅ 正常（enabled） |
| jyf | 2026-04-17 | 2026-04-06 | ✅ 正常（enabled） |
| 谢亚轩 | 2026-04-17 | 2026-04-15 | ✅ 正常（enabled） |
| 邱万惜 | 2026-04-17 | 2026-04-12 | ✅ 正常（enabled） |
| 江米小枣 | 2026-04-17 | 2026-04-16 | ✅ 正常（enabled） |

---

## 五、用户配置

共 **11 用户**，全部为当前启用用户（`enabled=true`）。2026-04-18 已从 `users.json` 删除全部停用用户配置。

> 密码和 PushPlus Token 均以「前4位+****+后4位」格式遮盖。

| 名称 | 佳明邮箱 | 账号区域 | 推送模式 | 密码（遮盖） | Token（遮盖） | 状态 | ICU分支 |
|---|---|---|---|---|---|---|---|
| 丛至 | 645042220@qq.com | 国际版 | self | Lcz1****529 | 5965****d002 | ✅ | **ICU实验分支** |
| Kevin | 656727039@qq.com | 国际版 | self | Kevi****123 | fa69****14ec | ✅ | 主分支 |
| 杨 | yangqihao@vip.qq.com | 国际版 | self | Yy19****10. | e960****e869 | ✅ | 主分支 |
| 未央 | 8616608773881 | 国内版 | friend | Heav****123 | 0eed****2ff | ✅ | 主分支 |
| 十月的麦子 | ychnjtech@163.com | 国内版 | friend | YCHt****001 | 1fc6****598e | ✅ | 主分支 |
| Cyanide | 1528192005@qq.com | 国内版 | friend | Song****004 | c2ff****492 | ✅ | 主分支 |
| 闲云野鹤 | 8613063635938 | 国内版 | friend | 2001****5Ht | 1c56****1b7e | ✅ | 主分支 |
| jyf | 296876549@qq.com | 国内版 | friend | Jia2****ming | b6f4****3e91 | ✅ | 主分支 |
| 谢亚轩 | 572748418@qq.com | 国内版 | friend | Xxwz****123 | 22a1****dc64 | ✅ | 主分支 |
| 邱万惜 | 8618030165218 | 国内版 | friend | TKFP****@qwx | 077f****73ac | ✅ | 主分支 |
| 江米小枣 | 849648717@qq.com | 国内版 | friend | 9582****bhY | eda2****187 | ✅（token空） | 主分支 |

**ICU 实验分支说明**：
- **仅「丛至」启用 ICU 实验功能**（`TARGET_USER_NAME = "丛至"` 硬编码在 `icu_cycling.py` 和 `icu_sleep.py`）
- ICU 骑行分析和 ICU 睡眠分析在 `garmin_monitor.py` 的主循环内单独调用
- 其余所有用户走主分支（Garmin 标准数据流）

---

## 六、实验分支：ICU + 骑行深度分析 路线图

### Phase 1 — 里程段拐点检测 【✅ 已完成】

拉 activity 的 streams.json（types=watts,heartrate,cadence），在代码里用滑动窗口检测"故事点"——主要是三类：持续高功率段（>FTP 的爬坡或主动进攻）、功率心率脱钩点（心率持续攀升但功率在降）、明显的衰减点（后半程同样功率心率比前半程高一个区间）。算法用 5 分钟滑动窗口，挑出最显著的 2-3 个点，带上里程位置（从 distance 流取）喂给大模型。不动天气、不动历史数据。

**当前已接入高德 API 做里程位置读取。**

**完成证据**：
- `app/icu_turning_points.py`：`detect_high_power_segments()`、`detect_decoupling_points()`、`detect_power_decay()` 全部实现，含5分钟滑动窗口
- `app/icu_cycling.py`：调用上述三个检测函数，并调用高德逆地理编码 API（`AMAP_REGEOCODE_URL`）
- `app/icu_turning_points_validator.py`：算法验证器已存在
- `/root/.env` 中 `AMAP_API_KEY` 已配置
- `/root/.icu_cycling_state.json` 中 `last_activity_id=i140263972`，说明已实际推送过

---

### Phase 2 — 风向分析 【✅ 已完成】

每 2km 计算一次骑行航向（GPS 方位角），调 Open-Meteo API 拉逐小时风向风速，做向量差分类（顺/侧/逆风）。输出分段详情 + 主导风力 + 气象概况，注入 LLM prompt 第三部分"天气与环境影响分析"。

**完成证据**：
- `icu_cycling.py` 中 `compute_bearing()`、`classify_wind_relation()`、`build_weather_segments()`、`build_weather_analysis()` 全部实现
- `fetch_open_meteo_weather()` 接入 Forecast/Archive 双端点
- streams 请求已包含 `latlng`、`altitude`
- 测试输出显示：56 分段，顺/侧/逆风各段均有计算结果

---

### Phase 3 — 历史基线对比 【✅ 已完成】

SQLite `data/丛至/rides.db` 存每次骑行关键指标。每次新骑行进来查距离 ±20%、IF ±15% 的历史可比记录，算差值后注入 LLM prompt"历史横向对比"段落。

**完成证据**：
- `app/ride_history.py`：`init_db()`、`upsert_ride()`、`find_comparable_rides()`、`compute_comparison()` 全部实现
- `icu_cycling.py` 推送后调用 `upsert_ride()` 写入历史库
- 推送前调用 `find_comparable_rides()` + `compute_comparison()` 生成对比数据
- 测试日志：`找到 1 次可对比历史骑行`

---

### Phase 4 — 知识库约束 【✅ 已完成，2026-04-18 大幅扩展】

将 Coggan、Friel、Seiler 权威训练方法论编码为决策规则表，写入知识库文件，强制 LLM 按规则出训练计划，禁止自由发挥。TSB 区间硬规则同时直接嵌入 prompt 的建议部分作为双重约束。

**知识库文件** `knowledge/cycling_training_plan.md`（**849 行，较原版 442 行扩展近一倍**）包含：
- 第1节：Coggan 7 功率区间表（%FTP、RPE、能量系统、典型时长）
- 第2节：PMC 框架 + TSB 决策表（7 档位 → 允许强度）
- 第3节：rampRate 安全表 + 恢复周触发条件（4 条任意一条触发）
- 第4节：Friel 周期化阶段（Prep→Base→Build→Peak→Race→Transition）
- 第5节：3+1 周节奏
- 第6节：Seiler 80/20 极化训练 + Z3 陷阱说明
- 第7节：HRV 红/黄/绿灯系统（rMSSD 阈值）
- 第8节：各课次规格（Z2/FTP 间歇/VO2max 4×4/Z1 恢复）
- **第9节：LLM 训练计划生成规则**（安全检查清单、阶段判定、周模板）
- 第10节：FTP 进步监测
- 第11节：过度训练预警信号
- ★**第11节：业余训练时间档位模板**（6h/8h/10h 三档，含周结构比例）
- ★**第12节：结构化课表处方库**（R1/E1/E2/S1/S2/S3/T1/V1/V2 规格）
- ★**第13节：HRV 指导训练决策树**（四灯系统：绿/黄/红/暗红，+20% 高端处理）
- ★**第14节：室内外功率校准与通勤负荷**（FTP 折算系数、通勤 Z1 不计 CTL）
- ★**第15节：业余常见错误与纠正**（FTP 虚高识别、跳过恢复周、永远中等强度等 5 类）
- ★**第16节：使用导航**（裁决规则优先级、跨库联动 sleep_health.md、禁止措辞清单）

`app/knowledge_helper.py` 骑行类型时注入第2、3、9、11、12、13、14、15、16节（**Activity prompt 从 ~12K 字扩展到 ~18K 字，~6110 tokens**）。

---

### 丛至专属系统 — 目标追踪 + 用户画像 【✅ 已完成，2026-04-18】

**目标追踪**（`goal_tracker.py`）：
- 每次分析推送后，LLM 提取文本中的前瞻建议存入 `daily_goals.json`
- 次日 ICU 睡眠分析时查询昨日目标，注入 prompt，LLM 可检查执行情况
- 日常观察写入 `observations_log.json`（90 天滚动，每 source 每天限一条）
- 三个推送点均已接入：晨报（`report_flow.py`）、ICU 睡眠（`icu_sleep.py`）、ICU 骑行（`icu_cycling.py`）

**用户长期画像**（`user_profile.py` + `weekly_profile_update.py`）：
- 画像文件：`data/丛至/user_long_term_profile.json`（5字段：训练模式、睡眠模式、恢复特征、个人观察、骑行进展）
- 每周日 7:00 cron 触发 `weekly_profile_update.py`，读取最近观察日志 + rides.db + 当前画像，LLM 综合合成新画像
- ICU 睡眠分析时 `profile_summary_for_prompt()` 注入 prompt，分析更加个性化

### 4.5 2026-04-18 审查修复后的运行状态

- **图表清理已修复并执行**：`scripts/prune_chart_images.sh` / `scripts/cleanup_charts.sh` 已改为扫描真实目录结构 `/var/www/garmin-assistant/public/charts/{user_id}/{message_type}/{date}/...`；执行一次真实清理后，图表文件数从 329 降至 327
- **health_runtime 旧用户残留已清理**：`app/garmin_monitor.py` 的 `update_health_runtime()` 已在接收最新 `enabled_users` 时裁剪 `per_user`；执行一次刷新后，`stale_per_user=[]`
- **主监控已重启加载修复**：新进程 PID `3128638`，启动时间 `2026-04-18T21:06:46+08:00`
- **新增回归测试**：`scripts/test_chart_cleanup_layout.py`、`scripts/test_health_runtime_pruning.py`

### 4.6 2026-04-18 停用用户资料清理后的状态

- **用户配置**：`config/users.json` 已从 20 条缩减为 11 条，仅保留当前启用用户；`disabled_count=0`
- **用户数据**：已删除停用用户的 `data/{name}`、`state/.garmin_monitor_state_{name}.json`、公开图表目录和可用 token 目录候选
- **聚合状态**：`state/garmin_rate_limit.json`、`state/health_followup_state.json` 已移除停用用户残留；`health_runtime.json` 仍为 11 个启用用户，`stale_per_user=[]`
- **历史记录**：已删除 16 个 `config/users.json.bak*` 旧配置备份；已清洗 `reports/daily` 中停用用户的日报条目；已从主运行日志中过滤停用用户行；已删除 62 个停用用户 activity LLM payload 快照
- **图表文件**：公开图表文件数清理后为 252

---

## 七、最近改动日志

以 git 状态、文件 mtime 和运行验证记录综合整理：

| 日期 | 描述 | 涉及文件 |
|---|---|---|
| 2026-04-18 | **停用用户资料与历史数据清理**：从 `users.json` 删除全部停用用户；删除对应 `data/`、`state/`、公开图表目录、旧配置备份和 activity LLM payload 快照；清洗日报和主运行日志；旧测试脚本去除停用用户硬编码 | `config/users.json`、`data/`、`state/`、`reports/daily/`、`logs/`、`scripts/test_*.py`、项目档案 |
| 2026-04-18 | **图表清理 TTL 修复**：清理脚本从错误的 `/charts/{type}` 改为真实 `/charts/{user_id}/{message_type}/{date}` 结构；支持 `BASE_DIR` 覆盖以便测试；真实清理后文件数 329→327 | `scripts/prune_chart_images.sh`、`scripts/cleanup_charts.sh`、`scripts/test_chart_cleanup_layout.py` |
| 2026-04-18 | **health_runtime 旧用户裁剪修复**：`update_health_runtime()` 在写入最新 `enabled_users` 时删除不再启用用户的 `per_user` 状态；刷新后 `stale_per_user=[]` | `app/garmin_monitor.py`、`scripts/test_health_runtime_pruning.py` |
| 2026-04-18 | **骑行知识库大幅扩展（442→849行）**：新增第11-16章，覆盖业余训练档位模板、课表处方库、四灯HRV决策树、室内外校准、常见错误纠正、跨库联动导航；knowledge_helper.py 同步接入新章节 | `knowledge/cycling_training_plan.md`、`app/knowledge_helper.py` |
| 2026-04-18 | **睡眠知识库大幅扩展（345→790行）**：新增第13-18章，覆盖多信号关联矩阵、时序基线偏差、睡眠结构深读、HRV分层解读、个体化基线校准、三层架构使用导航；A/B/C层结构，防止LLM混乱 | `knowledge/sleep_health.md` |
| 2026-04-18 | **天气误报 Bug 修复（根本原因）**：`elapsed_time`（秒）被误当分钟传入，8小时骑行→480小时→所有24小时纳入ride_hours→23点小雨混入。修复：`// 60` 转换 | `app/icu_cycling.py:1138` |
| 2026-04-18 | **天气降水噪声过滤**：新增 `_PRECIP_WEATHER_CN` 常量，降水类天气码需实际降水 ≥ 0.1mm 才记入天气状况（防御层） | `app/icu_cycling.py:528,772` |
| 2026-04-18 | **VirtualRide 纳入推送**：`select_latest_ride()` 从仅接受 `"ride"` 改为同时接受 `"virtualride"`，室内骑行可触发分析 | `app/icu_cycling.py:158` |
| 2026-04-18 | **SSH 配置修复**：创建 `~/.ssh/config`，配置 `garmin-aliyun` Host 别名（真实 IP `43.99.84.162` + 私钥 `aliyun_key`） | `~/.ssh/config`（本机） |
| 2026-04-18 | **清理 .bak 备份文件**：删除 `app/` 下 51 个历史备份文件 | `app/*.bak*` |
| 2026-04-18 | **新增骑行区间分布图**：替换密集轨迹图为双行堆叠横条（功率+心率区间），手机可读性大幅提升 | `app/chart_renderer.py`（新增 `render_cycling_zones_chart`）、`app/icu_cycling.py` |
| 2026-04-18 | **骑行推送升级为 HTML 图文**：`_push_message()` 支持 `chart_url`，使用 HTML template，图片置于文字前 | `app/icu_cycling.py` |
| 2026-04-18 | **新增目标追踪系统**：分析文本自动提取前瞻目标存队列，次日检查执行情况；三个推送点均已接入 | `app/goal_tracker.py`（新建）、`app/icu_cycling.py`、`app/icu_sleep.py`、`app/report_flow.py` |
| 2026-04-18 | **新增用户长期画像**：每周 LLM 综合合成画像，ICU 睡眠分析时注入 prompt | `app/user_profile.py`（新建）、`weekly_profile_update.py`（新建）、`app/icu_sleep.py` |
| 2026-04-18 | **Phase 3 历史基线对比**：SQLite rides.db 存骑行指标，可比骑行查询注入 LLM 分析 | `app/ride_history.py`（新建）、`app/icu_cycling.py` |
| 2026-04-17 | ICU 骑行深度分析持续迭代（含 Phase 2 风向分析上线） | `app/icu_cycling.py` |
| 2026-04-16 | 拐点检测算法更新 + 验证器完善 | `app/icu_turning_points.py`、`app/icu_turning_points_validator.py` |
| 2026-04-14 | 主监控器大版本更新（含 ICU 骑行/睡眠调度）；ICU 睡眠分析更新 | `app/garmin_monitor.py`、`app/icu_sleep.py` |
| 2026-04-04 | LLM helper、report_flow、phase1_builder 同步更新 | `app/llm_helper.py`、`app/report_flow.py`、`app/phase1_builder.py` |
| 2026-04-03 | analysis、weekly_monthly、chart_decider 批量调整；knowledge_helper 更新 | `analysis.py`、`weekly_monthly.py`、`app/chart_decider.py`、`app/knowledge_helper.py` |
| 2026-04-01 | app_config 用户重载逻辑调整 | `app/app_config.py` |
| 2026-03-31 | baseline、daily_snapshot、user_identity、scripts 重构 | 多文件 |
| 2026-03-17 | 早期补丁阶段 | `backup/patch*.py` |

---

## 八、遗留问题与 TODO

### 8.1 代码内注释

**无 TODO / FIXME / XXX 注释**（全项目 grep 结果为空）。

### 8.2 审计发现问题

#### 🔴 高优先级

| 编号 | 问题 | 位置 |
|---|---|---|
| S-01 | **Garmin 密码明文存储**：所有用户的 `garmin_password` 以明文写在 `users.json`，该文件无加密。若服务器被攻破，所有用户佳明账号同时泄露 | `config/users.json` |
| S-02 | **所有 API Key 明文存储**：LLM、飞书、PushPlus 的 key 全部明文写在 `system.json`；ICU 和高德 key 明文写在 `.env` | `config/system.json`、`/root/.env` |
| S-03 | **线上未提交改动较多**：Git 仓库已初始化，但生产目录存在多处 modified / untracked 文件，尚未形成可回滚快照 | 整个项目 |

#### 🟡 中优先级

| 编号 | 问题 | 位置 |
|---|---|---|
| M-01 | **十月的麦子睡眠数据长期缺失**：`dailySleepDTO 为空` 日志每个轮询周期都出现，且 CN API 频繁超时，但缺少告警/通知机制 | `app/garmin_monitor.py` + CN API |
| M-02 | **ICU 目标用户硬编码**：`TARGET_USER_NAME = "丛至"` 在 `icu_cycling.py` 和 `icu_sleep.py` 中硬编码，未来新增 ICU 用户需要改代码而非配置文件 | `app/icu_cycling.py:17`、`app/icu_sleep.py:16` |
| M-03 | **江米小枣 pushplus_token 为空字符串**：`users.json` 中 `pushplus_token: ""`，该用户 `push_mode=friend`，暂不影响推送，但空字符串未经验证拦截 | `config/users.json` |
| M-04 | **crontab 每15分钟触发但进程自持锁**：PID 文件机制每次 cron 触发都会产生一次进程启动再退出的开销，日志中大量 `已有进程在运行` 消息；可考虑改为 `systemd` 服务 | crontab + `app/garmin_monitor.py` |
| M-05 | **backup/ 目录遗留文件**：`backup/patch.py`、`patch_C.py`、`patch_monitor.py` 为旧版代码残留，含早期用户配置，应在建立 git 后删除或归档 | `backup/` |
| ~~M-06~~ | ~~**`.bak` 文件未清理**~~：**已解决 2026-04-18**，51 个 `.bak*` 文件已全部删除 | ✅ |
| ~~M-07~~ | ~~**图表清理脚本扫错目录，TTL 实际失效**~~：**已解决 2026-04-18**，脚本已按 `{user_id}/{message_type}/{date}` 扫描并补测试 | ✅ |
| ~~M-08~~ | ~~**health_runtime 只增不删，监控状态长期失真**~~：**已解决 2026-04-18**，按当前 `enabled_users` 裁剪 `per_user` 并补测试 | ✅ |
| ~~M-09~~ | ~~**停用用户资料和历史数据残留**~~：**已解决 2026-04-18**，停用用户配置、数据目录、state、公开图表、旧配置备份、日报条目和主运行日志已清理 | ✅ |

#### 🟢 低优先级 / 观察项

| 编号 | 问题 | 位置 |
|---|---|---|
| L-01 | **connectapi.garmin.cn 超时无重试上限**：CN 账号 API 超时后会自动进入下一轮询，但没有超时次数统计和告警，长期失联无感知 | `app/garmin_monitor.py` |
| L-02 | **`phase1_builder.py` 体积过大（120 KB）**：单文件包含睡眠/运动/周报/冷启动等所有 payload 构建逻辑，建议按功能拆分 | `app/phase1_builder.py` |
| L-03 | **`experiment.py` 用途不明**：文件名为实验脚本，16.5 KB，未在主流程中被调用，可能是废弃代码 | `experiment.py` |
| L-04 | **`generate_daily_report.py` 21:10 的触发时机**：日报 cron 固定 21:10，若晚间推送逻辑也在 garmin_monitor 里，两者可能存在部分重复 | crontab |

---

## 九、附录：项目目录结构快览

```
/root/garmin_assistant/
├── app/                          # 核心应用代码
│   ├── garmin_monitor.py         # 主监控进程（入口）
│   ├── phase1_builder.py         # LLM payload 构建（最大文件）
│   ├── report_flow.py            # 报告生成流程编排（含晨报目标提取）
│   ├── llm_helper.py             # LLM 调用封装 + 所有 prompts
│   ├── icu_cycling.py            # ICU 骑行深度分析（仅丛至）
│   ├── icu_sleep.py              # ICU 睡眠深度分析（仅丛至）
│   ├── icu_turning_points.py     # 骑行拐点检测算法
│   ├── icu_turning_points_validator.py
│   ├── goal_tracker.py           # 目标追踪 + 观察日志 ★新增
│   ├── user_profile.py           # 用户长期画像读写 ★新增
│   ├── ride_history.py           # 骑行历史 SQLite 库 ★新增
│   ├── activity_cleaner.py       # Garmin 活动数据清洗
│   ├── app_config.py             # 配置加载 + 脱敏工具
│   ├── garmin_cookie_client.py   # CN 账号 Cookie 登录客户端
│   ├── garmin_storage.py         # 数据包持久化
│   ├── knowledge_helper.py       # 知识库注入（骑行额外注入第2/3/9节）
│   ├── sleep_cleaner.py          # 睡眠数据清洗
│   ├── chart_renderer.py         # 图表渲染（折线/睡眠甜甜圈/骑行区间分布）
│   ├── chart_decider.py          # 图表触发决策
│   ├── chart_storage.py          # 图表文件管理
│   └── chart_config.py           # 图表配置
├── weekly_profile_update.py      # 每周画像更新脚本（cron 每周日 7:00）★新增
├── config/
│   ├── system.json               # 系统配置（LLM/飞书/PushPlus/监控参数）
│   └── users.json                # 用户配置（11 个启用用户，含密码/token）
├── state/                        # 每用户状态文件（11 个 JSON）
├── logs/
│   ├── garmin.log                # 主监控运行日志
│   ├── daily_report.log          # 日报脚本日志
│   └── garmin_monitor.log        # 补充日志
├── data/
│   └── 丛至/
│       ├── rides.db              # 骑行历史 SQLite ★新增
│       ├── daily_goals.json      # 目标队列 ★新增
│       ├── observations_log.json # 观察日志（90天滚动）★新增
│       └── user_long_term_profile.json  # 用户长期画像 ★新增
├── knowledge/
│   ├── cycling_training_plan.md  # Coggan/Friel/Seiler 骑行方法论知识库（849行，含第11-16章业余扩展）
│   ├── sleep_health.md           # 睡眠知识库（790行，含第13-18章深度解读层）
│   └── cycling/running/swimming/walking/trail_running.md
├── docs/                         # 内部文档
├── debug/                        # 调试样本
├── backup/                       # 历史补丁（应清理）
├── scripts/                      # 独立运行脚本
│   ├── prune_chart_images.sh      # 图表 TTL 清理（按 user/type/date 结构扫描）
│   ├── cleanup_charts.sh          # 图表清理兼容脚本
│   ├── test_chart_cleanup_layout.py
│   └── test_health_runtime_pruning.py
└── tokens/

/root/
├── .env                          # ICU + 高德 API Key
├── .icu_cycling_state.json       # ICU 骑行推送状态
├── .icu_sleep_state.json         # ICU 睡眠推送状态
└── garmin_tokens/                # Garmin auth token 缓存

/var/www/garmin-assistant/public/charts/  # 图表 HTTPS 托管目录
└── congzhi/activity/{date}/      # 骑行区间图（URL: https://43.99.84.162.sslip.io/charts/...）
```
